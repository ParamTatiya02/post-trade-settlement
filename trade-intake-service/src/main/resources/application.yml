server:
  port: 8081
spring:
  application:
    name: trade-intake-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/settlement_db}
    username: ${SPRING_DATASOURCE_USERNAME:settlement}
    password: ${SPRING_DATASOURCE_PASSWORD:settlement123}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
        ddl-auto: validate
    show-sql: true
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: 5672
    username: guest
    password: guest
rabbitmq:
  queues:
    intake-to-enrichment: intake-to-enrichment
jwt:
  secret: ${JWT_SECRET:1jRFQmWkwjwJMzMuYyLlDwKDJfnvfYK9u+LZc+czWVs=}